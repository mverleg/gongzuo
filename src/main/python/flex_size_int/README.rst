
Flex size int
===============================

This package encodes integers into a flexible sized format. The number of bytes grows as needed to support larger numbers. Small numbers take one byte, but numbers can grow as large as desired without losing precision.


